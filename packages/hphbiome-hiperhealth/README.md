# hphbiome-hiperhealth

HiperHealth skill adapter for the reusable `hphbiome` library.

This package provides the `HPHBiomeSkill` class and a `hiperhealth.skills` entry
point. It intentionally keeps HPH Biome logic in the standalone `hphbiome`
package and only adapts it to HiperHealth's skill API.
